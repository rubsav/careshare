<!DOCTYPE html>
<html>
<head>
  <%= favicon_link_tag 'favicon.ico' %>
  <title>CareShare</title>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href='http://fonts.googleapis.com/css?family=Quicksand:300,400,700' rel='stylesheet' type='text/css'>
</head>

<body class="w3-light-grey">
<header class="w3-display-container" id="home">
  <nav class="main-nav">
    <div class="w3-row w3-padding w3-black">
      <div class="w3-col s2">
        <%= link_to 'About', root_path, class: "w3-btn-block w3-hover-green"%>
      </div>
       <div class="w3-col s3">
        <%= link_to "Add & Rate your Caregiver", new_provider_path, class: "w3-btn-block w3-hover-green" %> 
      </div>
      <div class="w3-col s4">
        <%= link_to "View All Caregiving Providers", providers_path, class: "w3-btn-block w3-hover-green" %> 
      </div>
      <div class="w3-col w3-padding s3">
        <% if current_user %>
          Signed in as <%= link_to current_user.username, user_path(current_user) %>. <%= link_to "Log out", session_path("current"), :method => :delete %>
        <% else %>
          <%= link_to "Log in", new_session_path, class:"links" %> or <%= link_to "Sign up", new_user_path, class:"links" %>
        <% end %>
      </div>
    </div>
    
       <% if flash[:notice] %>
       <p id="notice"><%= flash[:notice] %></p>
       <% end %>

       <% if flash[:alert] %>
       <p id="alert"><%= flash[:alert] %></p>
       <% end %>
  </nav>
</header>
<div class="page-wrap">
<%= yield %>
</div>

<div class="w3-bottom w3-center site-footer">
  <p class="copyright">A review site by Ruben Avila Aguilar</p>
</div>

</body>
</html>
